<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coding.mybatis.UserMapper">
<!--    <cache readOnly="true" type=""/>-->

    <select id="selectById" resultType="com.coding.mybatis.User">
        select * from t_user_0 where id = #{id}
    </select>
</mapper>